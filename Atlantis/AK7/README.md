# JSON file for Atlantis AK7

The Atlantis AK7 source in https://github.com/peepeetee/qmk_firmware/tree/ak7/keyboards/atlantis/ak7 does not include a JSON file. I got this one from SpiderIsland on Ali Express and it works. They have it hosted on a Google Drive share which isn't searchable, so now it is. It's an old board so you have to load it with V2 definitions.
